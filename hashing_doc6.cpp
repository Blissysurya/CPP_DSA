#include <bits/stdc++.h>

using namespace std;

istream& operator>> ( istream& in,vector<int>& arr){
    for(int i=0;i<n;i++){
        in>>arr[i];
    }
    return in;
}

int main(){

    int n;
    cin>>n;

    int k;
    cin>>k;


    vector<int> vec(n);
    
    for(int i=0;i<n;i++){

    }
}